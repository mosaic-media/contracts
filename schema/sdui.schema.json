{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mosaic-media.github.io/mosaic-sdui/schema/sdui.schema.json",
  "title": "MosaicSDUI",
  "description": "The single source of truth for the Mosaic Server-Driven-UI contract. Language bindings (Go, TypeScript, Dart) are GENERATED from this file — do not hand-edit them. The root object exists only so a generator reaches every top-level type; the useful types are in $defs.",
  "type": "object",
  "properties": {
    "node": {
      "$ref": "#/$defs/UINode"
    },
    "action": {
      "$ref": "#/$defs/Action"
    },
    "definition": {
      "$ref": "#/$defs/ComponentDefinition"
    },
    "boxStyle": {
      "$ref": "#/$defs/BoxStyle"
    },
    "textStyle": {
      "$ref": "#/$defs/TextStyle"
    }
  },
  "additionalProperties": false,
  "$defs": {
    "UINode": {
      "title": "UINode",
      "description": "One element of a server-driven UI tree. The `type` is an open vocabulary: a client that does not recognise a type renders a placeholder rather than failing.",
      "type": "object",
      "required": [
        "type"
      ],
      "properties": {
        "type": {
          "type": "string",
          "description": "Component discriminator, e.g. \"PosterCard\". Namespaced: a core type — every primitive and every component in the published vocabulary — is unprefixed and never contains the type separator; a module's own type is \"moduleId:Type\". Open and flat is not the same as open: without the namespace two modules could both contribute a StatChip, and one could contribute a PosterCard and take the core component's place in every client's registry."
        },
        "id": {
          "type": "string",
          "description": "A stable identity for this node. It is the React key and the target of a narrow region update, and it is also the *analytics* identity: an onAppear action names what was seen by carrying whatever the server put here, so an id that is a row index attributes nothing and one that names the thing attributes everything. The server chooses it, because only the server knows what the node is about."
        },
        "props": {
          "type": "object",
          "additionalProperties": true,
          "description": "Component-specific data. Open by design. Any value may be a literal or a Binding (see #/$defs/Binding), which the client resolves where the node renders."
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/UINode"
          }
        },
        "slots": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "$ref": "#/$defs/UINode"
            }
          }
        }
      },
      "additionalProperties": false
    },
    "Tone": {
      "title": "Tone",
      "enum": [
        "neutral",
        "accent",
        "success",
        "warning",
        "danger",
        "info"
      ]
    },
    "Binding": {
      "title": "Binding",
      "description": "A prop value the client resolves where the node renders, rather than one decided when the tree was built. It names a path and nothing else — there is no formatting, default, expression or operator, because formatting stays server-side. An object is a Binding only when it has exactly this one key; carrying \"$bind\" beside anything else makes it a literal object, so a prop that happens to contain a field of that name is never reinterpreted. Any prop value may be one; a node's `type` may not, because a tree whose shape depends on data is a ComponentDefinition, not a screen.",
      "type": "object",
      "required": [
        "$bind"
      ],
      "properties": {
        "$bind": {
          "type": "string",
          "minLength": 1,
          "description": "The path to resolve. In this version of the vocabulary the only scope is the screen's params; later scopes resolve nearest-first, above that one."
        }
      },
      "additionalProperties": false
    },
    "Surface": {
      "title": "Surface",
      "enum": [
        "modal",
        "sheet",
        "drawer"
      ]
    },
    "ActionKind": {
      "title": "ActionKind",
      "description": "The behaviours a client interprets. Kept in step with ui.spec.json and proto/mosaic/sdui/v1/sdui.proto by `go run ./tools/genui -lint`, which fails when the three disagree — they had drifted to ten, nine and four before that gate existed.",
      "enum": [
        "navigate",
        "invoke",
        "playPart",
        "openUrl",
        "back",
        "query",
        "openOverlay",
        "closeOverlay",
        "toast",
        "sequence",
        "setValue",
        "submit"
      ]
    },
    "Action": {
      "title": "Action",
      "description": "A declarative behaviour envelope. Data, never code — the client interprets the kind. Each kind uses a subset of the fields.",
      "type": "object",
      "required": [
        "kind"
      ],
      "properties": {
        "kind": {
          "$ref": "#/$defs/ActionKind"
        },
        "screen": {
          "type": "string"
        },
        "params": {
          "type": "object",
          "additionalProperties": true
        },
        "url": {
          "type": "string"
        },
        "mutation": {
          "type": "string"
        },
        "input": {
          "type": "object",
          "additionalProperties": true
        },
        "surface": {
          "$ref": "#/$defs/Surface"
        },
        "node": {
          "$ref": "#/$defs/UINode"
        },
        "partId": {
          "type": "string"
        },
        "nodeId": {
          "type": "string"
        },
        "message": {
          "type": "string"
        },
        "tone": {
          "$ref": "#/$defs/Tone"
        },
        "field": {
          "type": "string",
          "description": "setValue: the name of the field written in the enclosing state scope. submit: where in the action input the collected values merge — a path into it, or absent for its top level."
        },
        "value": {
          "type": "string",
          "description": "setValue: the value written."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Action"
          }
        }
      },
      "additionalProperties": false
    },
    "ComponentDefinition": {
      "title": "ComponentDefinition",
      "description": "A component expressed as data: a name, default params, and a template of primitives. Clients register definitions and expand them; this is how a module contributes a component without shipping client code. A template node's props may hold binding objects ({\"$bind\":\"path\"} / {\"$match\":{…}}) and control keys ($if / $ifNot / $each / $as); a node of type \"Outlet\" renders the caller's children or a named slot.",
      "type": "object",
      "required": [
        "name",
        "template"
      ],
      "properties": {
        "name": {
          "type": "string",
          "description": "The node type this definition provides."
        },
        "params": {
          "type": "object",
          "additionalProperties": true,
          "description": "Default param values, overridden by the caller's props."
        },
        "template": {
          "$ref": "#/$defs/UINode"
        },
        "fallback": {
          "$ref": "#/$defs/UINode",
          "description": "An alternative template for a client whose declared vocabulary lacks a primitive the main template needs. The server picks per session and sends one; the client never sees both. Optional — a definition without one is served unchanged to everyone."
        }
      },
      "additionalProperties": false
    },
    "ColorToken": {
      "title": "ColorToken",
      "description": "A colour by ROLE, never a literal. The value behind it comes from the token set the Platform serves, so a re-skin changes no payload and no client.",
      "enum": [
        "bg",
        "surface",
        "surface-raised",
        "surface-overlay",
        "text",
        "text-muted",
        "text-faint",
        "text-on-accent",
        "accent",
        "accent-hover",
        "accent-quiet",
        "success",
        "warning",
        "danger",
        "info",
        "success-quiet",
        "warning-quiet",
        "danger-quiet",
        "info-quiet",
        "rating",
        "border",
        "border-strong"
      ]
    },
    "SpaceToken": {
      "title": "SpaceToken",
      "description": "A step on the spacing scale (0–9), or \"gutter\" — the fluid page margin that clamps with the viewport, so page padding is responsive without a breakpoint.",
      "oneOf": [
        {
          "type": "integer",
          "minimum": 0,
          "maximum": 9
        },
        {
          "const": "gutter"
        }
      ]
    },
    "RadiusToken": {
      "title": "RadiusToken",
      "description": "A corner radius from the scale.",
      "enum": [
        "sm",
        "md",
        "lg",
        "xl",
        "pill"
      ]
    },
    "ShadowToken": {
      "title": "ShadowToken",
      "description": "An elevation step.",
      "enum": [
        "1",
        "2",
        "3"
      ]
    },
    "TextVariant": {
      "title": "TextVariant",
      "description": "A step on the type scale.",
      "enum": [
        "xs",
        "sm",
        "md",
        "lg",
        "xl",
        "2xl",
        "3xl",
        "4xl"
      ]
    },
    "FontWeight": {
      "title": "FontWeight",
      "description": "A weight on the type scale.",
      "enum": [
        "regular",
        "medium",
        "bold"
      ]
    },
    "Dimension": {
      "title": "Dimension",
      "description": "A size: a number of pixels, \"full\" (100% of the parent), \"screen\" (the viewport in that axis — the one non-parent-relative size, for full-bleed surfaces and the app frame), \"auto\", or a percentage string.",
      "oneOf": [
        {
          "type": "number"
        },
        {
          "enum": [
            "full",
            "screen",
            "auto"
          ]
        },
        {
          "type": "string",
          "pattern": "^[0-9]+(\\\\.[0-9]+)?%$"
        }
      ]
    },
    "BoxStyle": {
      "title": "BoxStyle",
      "description": "Layout and surface styling for a Box, in TOKENS ONLY — no literal colours, no raw pixels except explicit dimensions. This is the technology-agnostic seam: it is the intersection of what a flexbox client and a Flutter client can render identically, so a definition written once renders the same everywhere. A client implements this vocabulary natively; growing it is the only change that requires a client release (ADR 0024).",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "layout": {
          "enum": [
            "flex",
            "grid"
          ],
          "description": "Layout mode. \"grid\" enables the grid-* fields."
        },
        "direction": {
          "enum": [
            "row",
            "column"
          ]
        },
        "gap": {
          "$ref": "#/$defs/SpaceToken"
        },
        "align": {
          "enum": [
            "start",
            "center",
            "end",
            "stretch",
            "baseline"
          ],
          "description": "Cross-axis alignment."
        },
        "justify": {
          "enum": [
            "start",
            "center",
            "end",
            "between",
            "around"
          ],
          "description": "Main-axis distribution."
        },
        "wrap": {
          "type": "boolean"
        },
        "gridMin": {
          "type": "number",
          "description": "grid: responsive auto-fill columns of at least this width in px."
        },
        "gridFlow": {
          "enum": [
            "row",
            "column"
          ],
          "description": "grid: flow direction; \"column\" makes a horizontal rail."
        },
        "gridAutoColumns": {
          "type": "number",
          "description": "grid: fixed track size for a rail, in px."
        },
        "gridColumns": {
          "type": "array",
          "description": "grid: an explicit column track list, for the arrangements auto-fill cannot state — a settings frame's nav/panel/aside, an episode row's fixed thumbnail beside a fluid title. A track is a number of pixels (fixed), \"auto\" (sized to its content), or {\"fill\": n} (n shares of what is left). Deliberately not a CSS grid-template string: these three are what a flexbox client and a Flutter client can both lay out identically.",
          "items": {
            "oneOf": [
              {
                "type": "number"
              },
              {
                "const": "auto"
              },
              {
                "type": "object",
                "required": [
                  "fill"
                ],
                "additionalProperties": false,
                "properties": {
                  "fill": {
                    "type": "number",
                    "exclusiveMinimum": 0
                  }
                }
              }
            ]
          }
        },
        "overflowX": {
          "enum": [
            "auto",
            "hidden",
            "visible"
          ]
        },
        "overflowY": {
          "enum": [
            "auto",
            "hidden",
            "visible"
          ]
        },
        "overflow": {
          "enum": [
            "hidden",
            "auto",
            "visible"
          ]
        },
        "snap": {
          "enum": [
            "x",
            "y"
          ],
          "description": "Scroll snapping axis, for carousels."
        },
        "snapAlign": {
          "enum": [
            "start",
            "center"
          ]
        },
        "p": {
          "$ref": "#/$defs/SpaceToken"
        },
        "px": {
          "$ref": "#/$defs/SpaceToken"
        },
        "py": {
          "$ref": "#/$defs/SpaceToken"
        },
        "pt": {
          "$ref": "#/$defs/SpaceToken"
        },
        "pr": {
          "$ref": "#/$defs/SpaceToken"
        },
        "pb": {
          "$ref": "#/$defs/SpaceToken"
        },
        "pl": {
          "$ref": "#/$defs/SpaceToken"
        },
        "bg": {
          "$ref": "#/$defs/ColorToken"
        },
        "bgGradient": {
          "type": "object",
          "description": "A linear gradient between two token colours (or \"transparent\"), at an angle in degrees.",
          "properties": {
            "from": {
              "$ref": "#/$defs/GradientStop"
            },
            "to": {
              "$ref": "#/$defs/GradientStop"
            },
            "angle": {
              "type": "number"
            }
          },
          "required": [
            "from",
            "to"
          ],
          "additionalProperties": false
        },
        "glass": {
          "type": "boolean",
          "description": "Render this surface in the acrylic material: translucent, blurred, and lit by the current light source (the focused artwork, or the brand light when there is none). A client without the material renders a plain translucent surface."
        },
        "scrim": {
          "enum": [
            "bottom",
            "top",
            "leading",
            "cinematic"
          ],
          "description": "A named legibility wash over artwork, so text laid on a backdrop stays readable whatever the image behind it. NAMED rather than a gradient the server writes, for the same reason colours are tokens: the recipe is a formula each client evaluates over its own tokens, and one written as literal stops would be a second skin the Platform owns. \"bottom\"/\"top\" fade an edge into the page; \"leading\" washes the text side (the direction follows the reading direction, so it mirrors in RTL); \"cinematic\" is both — the full-bleed hero treatment."
        },
        "glow": {
          "enum": [
            "art",
            "brand"
          ],
          "description": "A soft bloom cast over this box, screen-blended so it lights the artwork rather than tinting it flat. \"art\" draws the palette sampled from the focused image (the same source the acrylic material is lit by), so a hero glows in the colours of its own backdrop; \"brand\" uses the accent pair, for a surface with no artwork to sample. A client with no sampler renders \"art\" as \"brand\" rather than nothing."
        },
        "grain": {
          "type": "boolean",
          "description": "Overlay the material texture — the grain, blotch and scuff the token set carries — so a large flat surface reads as a material rather than as a fill. Soft-light blended over whatever is beneath, and purely decorative: it never affects layout or hit-testing."
        },
        "hoverGroup": {
          "type": "boolean",
          "description": "Mark this box as the region whose hover or focus reveals the `hoverReveal` boxes inside it. Stated explicitly, and on the ancestor rather than inferred from interactivity, because the thing revealed is frequently not inside the thing hovered: a rail's \"see all\" appears when the *section* is approached, and a link that appeared only while pointing at it could never be clicked."
        },
        "hoverReveal": {
          "type": "boolean",
          "description": "Hide this box until its nearest `hoverGroup` ancestor is hovered or focused, then fade it in. It is the card veil: the extra detail a tile shows on approach — time remaining, file size, a play affordance — which must not be in the resting composition and must not cost a second payload. An input model with no pointer reveals it on focus instead, and a client with neither renders it always-visible rather than never: unreachable detail is the worse failure."
        },
        "color": {
          "$ref": "#/$defs/ColorToken"
        },
        "radius": {
          "$ref": "#/$defs/RadiusToken"
        },
        "border": {
          "type": "boolean"
        },
        "borderColor": {
          "$ref": "#/$defs/ColorToken"
        },
        "borderSide": {
          "enum": [
            "top",
            "right",
            "bottom",
            "left"
          ],
          "description": "Draw `border` on one edge only — a rule under a row, a marker down the side of the selected item. Without it `border` draws all four."
        },
        "borderWidth": {
          "enum": [
            1,
            2
          ],
          "description": "The border's weight. Two steps, not a number: a hairline rule and a marker heavy enough to read as a selection. Anything thicker is a filled box, which this vocabulary already has."
        },
        "width": {
          "$ref": "#/$defs/Dimension"
        },
        "height": {
          "$ref": "#/$defs/Dimension"
        },
        "minWidth": {
          "$ref": "#/$defs/Dimension"
        },
        "maxWidth": {
          "$ref": "#/$defs/Dimension"
        },
        "minHeight": {
          "$ref": "#/$defs/Dimension"
        },
        "aspectRatio": {
          "type": "string",
          "description": "An aspect ratio as \"w / h\"."
        },
        "flex": {
          "type": "number"
        },
        "grow": {
          "type": "boolean"
        },
        "position": {
          "enum": [
            "relative",
            "absolute",
            "sticky",
            "fixed"
          ]
        },
        "top": {
          "$ref": "#/$defs/SpaceToken"
        },
        "right": {
          "$ref": "#/$defs/SpaceToken"
        },
        "bottom": {
          "$ref": "#/$defs/SpaceToken"
        },
        "left": {
          "$ref": "#/$defs/SpaceToken"
        },
        "overlap": {
          "$ref": "#/$defs/SpaceToken",
          "description": "Pull this box up over what precedes it by one step of the spacing scale, so a content sheet rides over the bottom of a full-bleed hero. A single direction and a token step, rather than negative margins in four directions: this is the one case in the layout where two siblings are meant to intersect, and naming it keeps it from becoming an arbitrary-offset escape hatch. Pair with `z` to say which one is in front."
        },
        "z": {
          "enum": [
            "raised",
            "overlay",
            "toast"
          ],
          "description": "Portable stack order for chrome that overlays content — not an arbitrary index."
        },
        "shadow": {
          "$ref": "#/$defs/ShadowToken"
        },
        "opacity": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "hidden": {
          "type": "boolean",
          "description": "Take the box out of the layout entirely. Its purpose is `responsive`: one payload carries both a desktop and a phone arrangement, and each viewport drops the half it does not use."
        },
        "responsive": {
          "type": "object",
          "description": "A style override applied below a viewport width — the vocabulary's one responsive capability, and what lets a layout adapt as DATA rather than through a client stylesheet. The override is a plain BoxStyle merged over the base: a field it does not mention keeps its base value, and null clears one (these travel as JSON, where an undefined member would vanish silently). One breakpoint, not a cascade.",
          "properties": {
            "below": {
              "type": "number",
              "description": "Viewport width in px, below which the override applies."
            },
            "style": {
              "$ref": "#/$defs/BoxStyle"
            }
          },
          "required": [
            "below",
            "style"
          ],
          "additionalProperties": false
        },
        "kind": {
          "type": "string",
          "description": "DEPRECATED in favour of `responsive`. A named hook a client may map to a rule of its own; it makes a layout depend on client CSS, which is what `responsive` exists to avoid."
        }
      }
    },
    "GradientStop": {
      "title": "GradientStop",
      "description": "A gradient endpoint: a colour token or \"transparent\".",
      "oneOf": [
        {
          "$ref": "#/$defs/ColorToken"
        },
        {
          "const": "transparent"
        }
      ]
    },
    "TextStyle": {
      "title": "TextStyle",
      "description": "Typography for a Text node, in tokens only.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "variant": {
          "$ref": "#/$defs/TextVariant"
        },
        "weight": {
          "$ref": "#/$defs/FontWeight"
        },
        "color": {
          "$ref": "#/$defs/ColorToken"
        },
        "align": {
          "enum": [
            "start",
            "center",
            "end"
          ]
        },
        "transform": {
          "enum": [
            "uppercase",
            "capitalize",
            "none"
          ]
        },
        "tracking": {
          "enum": [
            "tight",
            "normal",
            "wide"
          ],
          "description": "Letter-spacing: tight for display headings, wide for an eyebrow."
        },
        "italic": {
          "type": "boolean"
        },
        "mono": {
          "type": "boolean"
        },
        "tabular": {
          "type": "boolean",
          "description": "Tabular figures, so numbers in a column line up."
        },
        "lineClamp": {
          "type": "integer",
          "minimum": 1,
          "description": "Truncate after this many lines."
        },
        "shadow": {
          "type": "boolean",
          "description": "A legibility shadow behind the glyphs, for text laid directly over artwork with no scrim beneath it — a title on a tile, a caption on a still. Boolean rather than a shadow spec: the only question a payload gets to answer is whether the text is over an image, and how that is drawn is the client's."
        }
      }
    },
    "FieldError": {
      "title": "FieldError",
      "description": "One field's rejection. `field` is the field's name in the enclosing State scope — the same name the input that wrote it declares — so a rejection from the server renders exactly where a client-side validator's would. A name matching no field renders as a form-level error rather than being dropped, because a rejection nobody can see is worse than one in the wrong place.",
      "type": "object",
      "required": [
        "field",
        "message"
      ],
      "properties": {
        "field": {
          "type": "string"
        },
        "message": {
          "type": "string"
        }
      },
      "additionalProperties": false
    }
  }
}
